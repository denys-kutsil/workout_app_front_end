export const styles = {
  backgroundTop: { position: 'fixed', top: 0, left: 0, zIndex: -1 },
  backgroundBottom: { position: 'fixed', right: 0, bottom: 0, zIndex: -1 },
} as const;

export default styles;
